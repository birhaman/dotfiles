;; utils.el – Handle utility

;; Отображение карт
;; (require 'osm)
;;    (evil-define-key '(visual normal) osm-prefix-map
;;      (kbd "C-c m") #'osm-home)
;;   (with-eval-after-load 'org
;;      (require 'osm-ol))
